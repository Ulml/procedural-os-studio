# Any OpenAI-compatible endpoint (Ollama default — no Google).
LLM_BASE_URL=http://127.0.0.1:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=llama3.1

# Optional: commit selected Memory edits back to this GitHub repo.
GITHUB_TOKEN=
GITHUB_OWNER=Ulml
GITHUB_REPO=procedural-os-studio
GITHUB_BRANCH=main

# Optional MCP servers, one per line: name|command|arg1|arg2
# MCP_SERVERS=fs|npx|-y|@modelcontextprotocol/server-filesystem|/tmp

APP_HOST=127.0.0.1
APP_PORT=8787
